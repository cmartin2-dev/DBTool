<UserControl x:Class="DBTool.Controls.DropVersionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DBTool.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Style="{StaticResource commonLabel}">
            <TextBlock Text="{Binding tenantId}"/>
        </Label>
        <Button x:Name="btnGetSchema" Grid.Row="1" Grid.Column="0" Content="Get Versions"
                HorizontalAlignment="Left" Style="{StaticResource RoundButton}" Margin="5"
                Click="btnGetSchema_Click"/>
        <Button x:Name="btnDropSchema" Grid.Row="1" Grid.Column="1" Content="Drop Version"
                HorizontalAlignment="Right" Style="{StaticResource RoundButton}" Margin="5"
                Click="btnDropSchema_Click"/>
        <local:ListViewResultControl x:Name="lstResult" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                     Margin="5" Checkbox_Checked="lstResult_Checkbox_Checked" UnCheckbox_Checked="lstResult_UnCheckbox_Checked"/>
    </Grid>
</UserControl>
