<UserControl x:Class="DBTool.Controls.QueryPerSchemaControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DBTool.Controls"
             mc:Ignorable="d" 
             xmlns:avalon="http://icsharpcode.net/sharpdevelop/avalonedit"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Column="0" Grid.Row="0" Margin="5">
            <TabControl x:Name="schemaTab">
            </TabControl>
        </DockPanel>
        <GridSplitter Grid.Row="1" Grid.Column="0" HorizontalAlignment="Stretch"
VerticalAlignment="Stretch"
                Height="2" Margin="5"
Background="DarkGray"
ResizeDirection="Rows"
ShowsPreview="True"/>
        <DockPanel  Grid.Row="2" Grid.Column="0"  Margin="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <CheckBox x:Name="chkSCAH" Content="SCAH" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                <Button x:Name="btnExtract" Content="Extract All results" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" Margin="5" VerticalAlignment="Top" Style="{StaticResource RoundButton}" Click="btnExtract_Click"/>
                <local:TxtQueryControl x:Name="txtQueryControl" Grid.Row="1" Grid.Column="0" Execute="txtQueryControl_Execute" Grid.ColumnSpan="3"/>

            </Grid>
        </DockPanel>
        <GridSplitter Grid.Row="3" Grid.Column="0" HorizontalAlignment="Stretch"
VerticalAlignment="Stretch"
                Height="2" Margin="5"
Background="DarkGray"
ResizeDirection="Rows"
ShowsPreview="True"/>
        <TabControl x:Name="tabResult" Grid.Row="4" Grid.Column="0" Margin="5">
            
        </TabControl>
    </Grid>
</UserControl>
