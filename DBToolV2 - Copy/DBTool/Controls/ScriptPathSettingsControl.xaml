<UserControl x:Class="DBTool.Controls.ScriptPathSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DBTool.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Content="Script Path" Style="{StaticResource headerLabel}"/>
        <Label Grid.Row="1" Grid.Column="0" Content="Utility Path" Style="{StaticResource commonLabel}"/>
        <TextBox x:Name="txtUtilityPath" Text="{Binding UtilityPath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True"  Grid.Row="1" Grid.Column="1" />
        <Button x:Name="btnBrowse" Content="Browse" Grid.Column="2" Grid.Row="1" Grid.ColumnSpan="2" Margin="5,0,5,0" Style="{StaticResource RoundButton}" Click="btnBrowse_Click"/>
        <GroupBox Header="Paths" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" BorderBrush="Black" Margin="25">
            <Grid Margin="20,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Label Content="Create Folder Path" Grid.Row="0" Grid.Column="0" Margin="5" Style="{StaticResource commonLabel}"/>
                <Label Content="Upgrade Folder Path" Grid.Row="1" Grid.Column="0" Margin="5" Style="{StaticResource commonLabel}"/>

                <TextBox Name="txtCreateFolderPath" Text="{Binding CreatePath, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="5" IsReadOnly="True"/>
                <TextBox Name="txtUpgradeFolderPath" Text="{Binding UpgradePath, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="5" IsReadOnly="True"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
