<UserControl x:Class="DBTool.Controls.QueryListViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DBTool.Controls"
             xmlns:converters="clr-namespace:DBTool.Commons"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--<Label x:Name="lblTotalCount" Content="Total Count : 0" Grid.Column="0" Grid.Row="0"/>
        <StackPanel Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                --><!--<Button x:Name="btnExportToJson" Content="Export to JSON" Grid.Column="0" Grid.Row="0"
                    Click="btnExportToJson_Click"
                    Style="{StaticResource RoundButton}" Margin="5" Height="20"/>
                <Button x:Name="btnExportToExcel" Content="Export to Excel" Grid.Column="1" Grid.Row="0" 
                    Click="btnExportToExcel_Click"
                    Style="{StaticResource RoundButton}" Margin="5" Height="20"/>--><!--

            </Grid>
        </StackPanel>-->
        <local:ListViewResultControl x:Name="lstResult" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>
        <!--<ListView x:Name="lstViewResult" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"   Visibility="Hidden" 
              Margin="5,0,5,0"
                   VirtualizingStackPanel.IsVirtualizing="True"
          VirtualizingStackPanel.VirtualizationMode="Recycling"
          ScrollViewer.IsDeferredScrollingEnabled="True"
                  >
        </ListView>
        <Label x:Name="lblProgress" Content="Status : Idle" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="5,0,5,0"/>-->
        <!--<local:LoadingControl x:Name="loadingControl" Grid.Column="0" Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="2"/>-->
    </Grid>
</UserControl>
